#ifndef GENERATE_HPP
#define GENERATE_HPP

#include <functional>
#include <memory>
#include <string>
#include <vector>

namespace baselib {

	int sum_py(double a, double b);

} // namespace baselib

#endif // GENERATE_HPP
